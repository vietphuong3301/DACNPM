@model DACNPM.Models.Category

@{
    ViewData["Title"] = "Create Category";
}
<div class="section mt-5">
    <div class="container">
        <div class="card mb-4 mt-5">
            <div class="card-header">
                <i class="fas fa-shopping-cart"></i>
                <strong>@ViewBag.Title</strong>
            </div>
            <div class="card-body">

                <div class="row">
                    <div class="col-md-12 offset-1">
                        <form asp-controller="Categories" asp-action="Create" class="row g-3">
                            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                            <input type="hidden" asp-for="CategoryID" value="@ViewBag.CategoryID" />

                            <div class="col-md-5">
                                <div class="form-group mb-3">
                                    <label asp-for="CategoryName" class="control-label"></label>
                                    <input asp-for="CategoryName" class="form-control" />
                                    <span asp-validation-for="CategoryName" class="text-danger"></span>
                                </div>

                                <div class="form-group">
                                    <a class="btn btn-secondary" asp-controller="Categories" asp-action="Index">Show All</a>
                                    <input type="submit" value="Create" class="btn btn-success float-end" />
                                </div>


                            </div>
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}
